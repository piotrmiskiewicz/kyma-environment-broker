name: Promote KEB to dev

env:
  MANAGEMENT_PLANE_CHARTS:  "https://${{ vars.GH_TOOLS_HOST }}/kyma/${{ vars.MP_CHARTS_REPO_NAME }}.git"
  KEB_API_URL: "https://api.github.com/repos/kyma-project/kyma-environment-broker"
  KEB_RELEASES_URL: "https://github.com/kyma-project/kyma-environment-broker/releases"

on:
  workflow_dispatch:
    inputs:
      releaseTag:
        description: "Release Tag"
        default: 'use latest release'
        required: true
  workflow_call:
    inputs:
      releaseTag:
        type: string
        description: "Release Tag"
        default: 'use latest release'
        required: true

jobs:
  promote:
    runs-on: ubuntu-latest

    steps:
      - name: Validate required environment variables
        run: |
          [ -z "${{ vars.GH_TOOLS_HOST }}" ] && echo "GH_TOOLS_HOST is required" && exit 1
          [ -z "${{ vars.GIT_TOOLS_USER_EMAIL }}" ] && echo "GIT_TOOLS_USER_EMAIL is required" && exit 1
          [ -z "${{ vars.GIT_USER_NAME }}" ] && echo "GIT_USER_NAME is required" && exit 1
          [ -z "${{ vars.MP_CHARTS_REPO_NAME }}" ] && echo "MP_CHARTS_REPO_NAME is required" && exit 1
          echo "Validated"

      - name: Generate enterprise bot token
        id: enterprise-bot-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ vars.GOPHER_TOOLS_BOT_APP_ID }}
          private-key: ${{ secrets.GOPHER_TOOLS_BOT_APP_PRIVATE_KEY }}
          github-api-url: https://${{ vars.GH_TOOLS_HOST }}/api/v3
          owner: kyma
          repositories: management-plane-charts,release-management

      - name: Use default (latest) Release Tag
        if: inputs.releaseTag == 'use latest release'
        run: |
          latest=$(curl -s -H "Accept: application/vnd.github+json" ${KEB_API_URL}/releases/latest | jq -r '.tag_name')
          if [ -z "$latest" ]; then
              echo "Failed to fetch the latest release tag"
              exit 1
          fi
          echo "TAG=${latest}" >> $GITHUB_ENV

      - name: Validate given release tag
        if: inputs.releaseTag != 'use latest release'
        env:
          INPUT_TAG: ${{ inputs.releaseTag }}
        run: |
          tags=$(curl -s -H "Accept: application/vnd.github+json" ${KEB_API_URL}/tags | jq -r '.[] | .name')

          if echo "$tags" | tr " " '\n' | grep -F -q -x "$INPUT_TAG"; then
              echo "TAG=$INPUT_TAG" >> $GITHUB_ENV
              echo "tag found"
          else
              echo "tag not found: $INPUT_TAG"
              exit 1
          fi

      - name: Set branch name
        run: echo "BRANCH_NAME=keb-${TAG}" >> $GITHUB_ENV

      - name: Setup git and clone repo
        env:
          MANAGEMENT_PLANE_CHARTS_WITH_USER: "https://x-access-token:${{ steps.enterprise-bot-token.outputs.token }}@${{ vars.GH_TOOLS_HOST }}/kyma/${{ vars.MP_CHARTS_REPO_NAME }}.git"
        run: |
          git config --global user.email "${{ vars.GIT_TOOLS_USER_EMAIL }}"
          git config --global user.name "${{ vars.GIT_USER_NAME }}"
          git clone "${MANAGEMENT_PLANE_CHARTS_WITH_USER}"

      - name: Setup python
        uses: actions/setup-python@v7
        with:
          python-version: '3.9'
          cache: 'pip'

      - name: Install requirements
        working-directory: management-plane-charts
        run: pip install -r tools/requirements.txt

      - name: Bump keb version
        working-directory: management-plane-charts
        run: |
          git checkout chart/keb-sap
          git checkout -B ${BRANCH_NAME}
          make bump-patch
          yq -i '.dependencies |= map(select(.name == "keb").version="'${TAG}'")' keb-sap/Chart.yaml
          yq e ".appVersion = \"${TAG}\"" -i keb-sap/Chart.yaml
        
      - name: Check changes
        working-directory: management-plane-charts
        run: |
          changes=$(git diff --ignore-all-space --shortstat)
          echo "$changes"
          if [[ "$changes" != *"3 insertions(+), 3 deletions(-)"* ]]; then
            echo "Expected 3 insertions and 3 deletions, but found different changes."
            exit 1
          fi

      - name: Commit and push changes
        working-directory: management-plane-charts
        env:
          MANAGEMENT_PLANE_CHARTS_WITH_USER: "https://x-access-token:${{ steps.enterprise-bot-token.outputs.token }}@${{ vars.GH_TOOLS_HOST }}/kyma/${{ vars.MP_CHARTS_REPO_NAME }}.git"
        run: |
          git add .
          git commit -m "Bump keb to ${TAG}"
          git remote set-url origin "${MANAGEMENT_PLANE_CHARTS_WITH_USER}"
          git push origin ${BRANCH_NAME}

      - name: Create PR if needed
        working-directory: management-plane-charts
        env:
          MANAGEMENT_PLANE_CHARTS_REPO_URL: "https://${{ vars.GH_TOOLS_HOST }}/kyma/${{ vars.MP_CHARTS_REPO_NAME }}"
          GH_ENTERPRISE_TOKEN: ${{ steps.enterprise-bot-token.outputs.token }}
          # needed by gh cli for GitHub enterprise
          GH_HOST: ${{ vars.GH_TOOLS_HOST }}
        run: |
          prs=$(gh pr list -R "${MANAGEMENT_PLANE_CHARTS_REPO_URL}" -A app/kyma-gopher --state open --json headRefName | jq -r '.[] | .headRefName')

          if  echo $prs | tr " " '\n' | grep -F -q -x ${BRANCH_NAME}; then
              echo "PR already exists, no need to create new one"
              pr_url=$(gh pr list -R "${MANAGEMENT_PLANE_CHARTS_REPO_URL}" --state open --json headRefName,url | jq -r --arg branch "${BRANCH_NAME}" '.[] | select(.headRefName==$branch) | .url')
          else
              pr_url=$(printf '%s\nRisk is Low\n' "${{env.KEB_RELEASES_URL }}/${TAG}" | gh pr create -B chart/keb-sap -H ${BRANCH_NAME} -R https://${{ vars.GH_TOOLS_HOST }}/kyma/${{ vars.MP_CHARTS_REPO_NAME }}/ --title "Bump keb to ${TAG}" --fill --body-file -)
              echo "${pr_url}" | tee $GITHUB_STEP_SUMMARY
          fi
          echo "PROMOTE_PR_URL=${pr_url}" >> $GITHUB_ENV

      - name: Comment on release management issue for patch releases
        working-directory: management-plane-charts
        env:
          MANAGEMENT_PLANE_CHARTS_REPO_URL: "https://${{ vars.GH_TOOLS_HOST }}/kyma/${{ vars.MP_CHARTS_REPO_NAME }}"
          RELEASE_MANAGEMENT_REPO_URL: "https://${{ vars.GH_TOOLS_HOST }}/kyma/release-management"
          GH_ENTERPRISE_TOKEN: ${{ steps.enterprise-bot-token.outputs.token }}
          # needed by gh cli for GitHub enterprise
          GH_HOST: ${{ vars.GH_TOOLS_HOST }}
        run: |
          patch=$(echo "${TAG}" | cut -d. -f3)
          if [ "${patch}" = "0" ]; then
            echo "Feature release, skipping release management comment"
            exit 0
          fi

          major_minor=$(echo "${TAG}" | cut -d. -f1-2)
          feature_tag="${major_minor}.0"

          chart_version=$(gh pr list -R "${MANAGEMENT_PLANE_CHARTS_REPO_URL}" --state all --search "Bump keb to ${feature_tag} in:title" --json number,mergedAt --jq 'sort_by(.mergedAt) | last | .number' | xargs -I{} gh api "/repos/kyma/${{ vars.MP_CHARTS_REPO_NAME }}/pulls/{}/files" --hostname ${{ vars.GH_TOOLS_HOST }} | jq -r '.[] | select(.filename | endswith("Chart.yaml")) | .patch' | grep '^\+version:' | head -1 | sed 's/^+version: *//')

          if [ -z "${chart_version}" ]; then
            echo "Could not determine chart version from feature PR, skipping comment"
            exit 1
          fi

          rm_issue_number=$(gh issue list -R "${RELEASE_MANAGEMENT_REPO_URL}" --state all --search "kyma-project.io/kyma-runtime/kcp-components/keb-sap ${chart_version} in:title" --json number --jq '.[0].number')

          if [ -z "${rm_issue_number}" ]; then
            echo "No release management issue found for chart version ${chart_version}, skipping comment"
            exit 1
          fi

          gh issue comment "${rm_issue_number}" -R "${RELEASE_MANAGEMENT_REPO_URL}" --body "Patch ${TAG} is approved here: ${PROMOTE_PR_URL}"
